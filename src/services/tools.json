[
  {
    "name": "run_command",
    "description": "Execute a shell command on the user's computer. Use this to run scripts, install packages, or perform system administration tasks. BE CAREFUL with destructive commands.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The command line to execute."
        }
      },
      "required": ["command"]
    }
  },
  {
    "name": "read_file",
    "description": "Read the contents of a file from the local file system.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The absolute path to the file."
        }
      },
      "required": ["path"]
    }
  },
  {
    "name": "write_file",
    "description": "Write content to a file. Can be used to create new files or overwrite existing ones.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The absolute path to the file."
        },
        "content": {
          "type": "string",
          "description": "The content to write."
        },
        "append": {
          "type": "boolean",
          "description": "If true, append the content to the existing file instead of overwriting it."
        }
      },
      "required": ["path", "content"]
    }
  },
  {
    "name": "move_file",
    "description": "Move or rename a file from a source path to a destination path.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "The current absolute path of the file."
        },
        "destination": {
          "type": "string",
          "description": "The new absolute path for the file."
        }
      },
      "required": ["source", "destination"]
    }
  },
  {
    "name": "copy_file",
    "description": "Copy a file from a source path to a destination path.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "The absolute path of the source file."
        },
        "destination": {
          "type": "string",
          "description": "The new absolute path for the destination file."
        }
      },
      "required": ["source", "destination"]
    }
  },
  {
    "name": "delete_file",
    "description": "Delete a file from the local file system. USE WITH EXTREME CAUTION.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The absolute path of the file to delete."
        }
      },
      "required": ["path"]
    }
  },
  {
    "name": "make_directory",
    "description": "Create a new directory at the specified path.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The absolute path of the directory to create."
        }
      },
      "required": ["path"]
    }
  },
  {
    "name": "list_directory",
    "description": "List the files and subdirectories within a given directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "The absolute path to the directory."
        }
      },
      "required": ["path"]
    }
  },
  {
    "name": "get_system_info",
    "description": "Retrieve information about the system (OS, memory, CPU, etc.).",
    "parameters": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "open_app",
    "description": "Launch a desktop application by name.",
    "parameters": {
      "type": "object",
      "properties": {
        "app_name": {
          "type": "string",
          "description": "The name of the application to open."
        }
      },
      "required": ["app_name"]
    }
  },
  {
    "name": "close_app",
    "description": "Close a running application by name.",
    "parameters": {
      "type": "object",
      "properties": {
        "app_name": {
          "type": "string",
          "description": "The name of the application to close."
        }
      },
      "required": ["app_name"]
    }
  },
  {
    "name": "get_clipboard_content",
    "description": "Read the current content of the system clipboard.",
    "parameters": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "set_clipboard_content",
    "description": "Set the content of the system clipboard.",
    "parameters": {
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "description": "The text to copy to the clipboard."
        }
      },
      "required": ["content"]
    }
  },
  {
    "name": "send_notification",
    "description": "Display a desktop notification.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Notification title."
        },
        "message": {
          "type": "string",
          "description": "Notification message body."
        }
      },
      "required": ["title", "message"]
    }
  },
  {
    "name": "create_skill",
    "description": "Create a new skill for the agent. This saves a markdown file in the skills directory that defines how the agent should handle a specific task in the future. Skill files should include triggers and specific command steps.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the skill (e.g., 'Search Google', 'Analyze Logs')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of what the skill does."
        },
        "instructions": {
          "type": "string",
          "description": "The specific markdown content of the skill, including steps and examples."
        },
        "filename": {
          "type": "string",
          "description": "The filename to save as (e.g., 'search_google.md'). Must end in .md."
        }
      },
      "required": ["name", "description", "instructions", "filename"]
    }
  },
  {
    "name": "get_skill_details",
    "description": "Fetch the full instructions and implementation details for a specific skill. Use this when you need to perform a task that matches a skill's description but you don't have the full instructions in your current context.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill_name": {
          "type": "string",
          "description": "The name of the skill folder (e.g., 'web_search', 'email')."
        }
      },
      "required": ["skill_name"]
    }
  },
  {
    "name": "update_config",
    "description": "Update the agent's configuration or save a secret (like an API key). Secrets will be encrypted. The updated value is available immediately in the environment.",
    "parameters": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "The configuration key (e.g., 'SERPER_API_KEY', 'DEFAULT_MODEL')."
        },
        "value": {
          "type": "string",
          "description": "The value to save."
        },
        "isSecret": {
          "type": "boolean",
          "description": "Whether to encrypt the value before saving."
        }
      },
      "required": ["key", "value", "isSecret"]
    }
  }
]
